import 'package:flutter/material.dart';
import 'package:flutter/widgets.dart';

Widget returnHeadTitle(String text) {
  return Text(
    text,
    style: TextStyle(
      color: Colors.white,
      fontFamily: "Poppins",
    ),
  );
}
